<UserControl x:Class="ImageCombinerChannelExtractor.Components.UserControls.DoingTaskNotif"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" >
    <Grid HorizontalAlignment="Right" Background="#FF3D3D88">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="Icon" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" Text="&#xE82F;" Foreground="White" FontSize="24" Margin="5,0,0,0" />
        <TextBlock Grid.Column="1" x:Name="MessageText" Foreground="White" FontSize="18" TextWrapping="Wrap" Text="Default message" VerticalAlignment="Center" Margin="5,5,5,5"/>
    </Grid>
</UserControl>
