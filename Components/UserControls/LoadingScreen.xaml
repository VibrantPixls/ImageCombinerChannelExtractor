<UserControl x:Class="ImageCombinerChannelExtractor.Components.UserControls.LoadingScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageCombinerChannelExtractor.Components.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="700" ScrollViewer.VerticalScrollBarVisibility="Disabled">
    <Grid Background="#FFFF8181">
        <StackPanel ScrollViewer.VerticalScrollBarVisibility="Disabled" VerticalAlignment="Center" Margin="25,0,25,0">
            <Label x:Name="lblLoadingText" Content="Loading..." Background="{x:Null}" FontSize="24" HorizontalContentAlignment="Center"/>
            <ProgressBar x:Name="progressBarLoading" HorizontalContentAlignment="Stretch" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>
